"""PMM package: persistent, model-agnostic AI mind (refactor scaffold).

This skeleton provides modules for storage (event log + projection),
commitment tracking, LLM adapters via a unified factory, a bridge for
styling, and a unified runtime loop. Implementations will be added
incrementally in subsequent steps.
"""
