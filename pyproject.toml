[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pmm"
version = "2.0.0"
description = "Persistent Mind Model v2"
requires-python = ">=3.9"

[project.scripts]
pmm = "pmm_v2.runtime.cli:main"
